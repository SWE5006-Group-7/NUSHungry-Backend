# Server Configuration
server.port=8081
# server.servlet.context-path=/api  # 移除 context-path，Controller 中已定义完整路径

# Application Configuration
spring.application.name=user-service

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/nushungry_users
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.orm.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=mySecretKeyForNUSHungryUserServiceThatIsLongEnoughForHS256Algorithm
jwt.access-token.expiration=3600000
jwt.refresh-token.expiration=2592000000

# Logging Configuration
logging.level.com.nushungry.userservice=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%thread] %-5level [%logger{0}] - %msg%n
logging.pattern.file=logs/user-service.%d{yyyy-MM-dd}.log
logging.file.max-size=10MB
logging.pattern.file.max-history=30

# Management Endpoints
management.endpoints.web.exposure.include=*

# Email Configuration (for development)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nushungry@gmail.com
spring.mail.password=qzjbtgoueyordfeh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Verification Code Configuration
app.verification.code.expiration=300000
app.verification.code.length=6

# Microservices URLs
services.cafeteria-service.url=http://localhost:8083/api
services.review-service.url=http://localhost:8084/api
services.preference-service.url=http://localhost:8085/api
services.media-service.url=http://localhost:8086/api

# Feign Client Configuration
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=10000
feign.client.config.default.loggerLevel=basic
